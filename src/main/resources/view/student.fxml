<?xml version="1.0" encoding="UTF-8"?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.control.*?>
<?import javafx.geometry.Insets?>
<?import javafx.scene.image.ImageView?>
<?import javafx.scene.image.Image?>

<AnchorPane xmlns="http://javafx.com/javafx"
            xmlns:fx="http://javafx.com/fxml"
            fx:controller="com.app.desktopapp.controller.StudentController"
            stylesheets="@../css/student.css"
            styleClass="content-pane">
    <VBox spacing="20" AnchorPane.topAnchor="20" AnchorPane.bottomAnchor="20" AnchorPane.leftAnchor="20" AnchorPane.rightAnchor="20">
        <HBox alignment="CENTER_LEFT" spacing="20">
            <Label text="📚 Quản lý Sinh viên" styleClass="header-title"/>
            <HBox HBox.hgrow="ALWAYS"/>
            <Button onAction="#handleRefresh" styleClass="btn-refresh">
                <graphic>
                    <ImageView>
                        <Image url="@../images/refresh.png" />
                    </ImageView>
                </graphic>
            </Button>
        </HBox>

        <VBox spacing="15">
            <HBox spacing="12" alignment="CENTER_LEFT">
                <TextField fx:id="txtSearch" promptText="🔍 Tìm kiếm sinh viên theo MSSV, tên, email..."
                          HBox.hgrow="ALWAYS" styleClass="search-field"/>
                <Button onAction="#handleSearch" text="Tìm kiếm" styleClass="btn-search"/>
                <Button onAction="#handleAddStudent" text="+ Thêm sinh viên" styleClass="btn-add"/>
            </HBox>
            
            <HBox spacing="10">
                <Button onAction="#handleEditStudent" text="✏️ Chỉnh sửa" styleClass="btn-edit" disable="true" fx:id="btnEdit"/>
                <Button onAction="#handleDeleteStudent" text="🗑️ Xóa" styleClass="btn-delete" disable="true" fx:id="btnDelete"/>
                <Button onAction="#handleViewDetails" text="👁️ Chi tiết" styleClass="btn-view" disable="true" fx:id="btnView"/>
                <HBox HBox.hgrow="ALWAYS"/>
                <Label text="Hiển thị: " styleClass="info-text"/>
                <Label text="0/0 sinh viên" styleClass="info-text" fx:id="lblRecordCount"/>
            </HBox>
        </VBox>

        <TableView fx:id="tableStudent" styleClass="table-view" prefHeight="450" onMouseClicked="#handleTableClick" VBox.vgrow="ALWAYS">
            <columns>
                <TableColumn fx:id="colStudentCode" text="MSSV" prefWidth="90" resizable="true"/>
                <TableColumn fx:id="colName" text="Họ tên" prefWidth="180" resizable="true"/>
                <TableColumn fx:id="colEmail" text="Email" prefWidth="200" resizable="true"/>
                <TableColumn fx:id="colMajor" text="Chuyên ngành" prefWidth="130" resizable="true"/>
                <TableColumn fx:id="colDob" text="Ngày sinh" prefWidth="110" resizable="true"/>
                <TableColumn fx:id="colStartYear" text="Khóa" prefWidth="80" resizable="true"/>
                <TableColumn fx:id="colGender" text="Giới tính" prefWidth="90" resizable="true"/>
            </columns>
            <columnResizePolicy>
                <TableView fx:constant="CONSTRAINED_RESIZE_POLICY"/>
            </columnResizePolicy>
        </TableView>

        <HBox alignment="CENTER_RIGHT" spacing="15">
            <Label text="📊 Thống kê: " styleClass="info-text"/>
            <Label text="Nam: 0 | Nữ: 0" styleClass="info-text" fx:id="lblGenderStats"/>
            <Label text="CNTT: 0 | KT: 0 | QTKD: 0" styleClass="info-text" fx:id="lblMajorStats"/>
        </HBox>
    </VBox>
</AnchorPane>
